<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="App_Gestion_lots_M3.UI.Pages.StatistiquesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Statistiques" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock
                Text="Synthèse des lots par état sur la période sélectionnée"
                Style="{StaticResource BodyTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <Border
            Grid.Row="1"
            Padding="16"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="{StaticResource ControlCornerRadius}">
            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                <TextBlock Text="Période :" VerticalAlignment="Center" />
                <ComboBox
                    x:Name="cboPeriode"
                    MinWidth="160"
                    SelectedIndex="2">
                    <ComboBoxItem Content="Jour" />
                    <ComboBoxItem Content="Semaine" />
                    <ComboBoxItem Content="Mois" />
                </ComboBox>
                <TextBlock Text="Du :" VerticalAlignment="Center" />
                <CalendarDatePicker x:Name="dtpDu" />
                <TextBlock Text="Au :" VerticalAlignment="Center" />
                <CalendarDatePicker x:Name="dtpAu" />
                <Button
                    x:Name="btnActualiser"
                    Content="Actualiser"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="btnActualiser_Click" />
            </StackPanel>
        </Border>

        <Border
            Grid.Row="2"
            Padding="24"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="{StaticResource ControlCornerRadius}"
            VerticalAlignment="Top">
            <Grid RowSpacing="12" ColumnSpacing="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="240" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Lots par état" Style="{StaticResource SubtitleTextBlockStyle}" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Total :" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBlock x:Name="lblTotal" Grid.Row="1" Grid.Column="1" Text="0" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="En attente :" />
                <TextBlock x:Name="lblEnAttente" Grid.Row="2" Grid.Column="1" Text="0" />

                <TextBlock Grid.Row="3" Grid.Column="0" Text="En production :" />
                <TextBlock x:Name="lblEnProduction" Grid.Row="3" Grid.Column="1" Text="0" />

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Terminés :" />
                <TextBlock x:Name="lblTermines" Grid.Row="4" Grid.Column="1" Text="0" />

                <TextBlock Grid.Row="5" Grid.Column="0" Text="En erreur :" />
                <TextBlock x:Name="lblEnErreur" Grid.Row="5" Grid.Column="1" Text="0" />
            </Grid>
        </Border>
    </Grid>
</Page>
