<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="App_Gestion_lots_M3.UI.Pages.LotsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:local="using:App_Gestion_lots_M3.UI.Pages"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Lots" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock
                Text="Liste des lots de production"
                Style="{StaticResource BodyTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <InfoBar
            x:Name="StatusInfoBar"
            Grid.Row="1"
            IsClosable="True"
            IsOpen="False" />

        <!-- Un seul DataTemplate, pas de sélecteur de template, pas de
             Page.Resources. Les chemins x:Bind à propagation null
             (Lot.X / Evenement.X) font qu'au sein d'une cellule, seul le
             <Run> dont la racine n'est pas null produit du texte ;
             l'autre se replie sur FallbackValue=''. -->
        <controls:HeaderedTreeView
            x:Name="treeLots"
            Grid.Row="2"
            SelectionMode="Single"
            DoubleTapped="treeLots_DoubleTapped"
            ItemsSource="{x:Bind _rows, Mode=OneWay}">
            <controls:HeaderedTreeView.Header>
                <controls:DataTable ColumnSpacing="16">
                    <controls:DataColumn Content="Nom du Lot" DesiredWidth="160" />
                    <controls:DataColumn Content="Quantité"   DesiredWidth="120" />
                    <controls:DataColumn Content="Recette"    DesiredWidth="150" />
                    <controls:DataColumn Content="État"       DesiredWidth="Auto" />
                </controls:DataTable>
            </controls:HeaderedTreeView.Header>
            <controls:HeaderedTreeView.ItemTemplate>
                <DataTemplate x:DataType="local:LotTreeNode">
                    <muxc:TreeViewItem ItemsSource="{x:Bind Children}">
                        <controls:DataRow HorizontalAlignment="Left">
                            <!-- Cellule 1 : nom du lot OU date formatée de l'événement -->
                            <TextBlock VerticalAlignment="Center">
                                <Run Text="{x:Bind Lot.LOT_Nom, Mode=OneWay, FallbackValue=''}" />
                                <Run Text="{x:Bind local:LotsPage.FormatDate(Evenement), Mode=OneWay, FallbackValue=''}" />
                            </TextBlock>
                            <!-- Cellule 2 : quantité du lot, vide pour les événements -->
                            <TextBlock VerticalAlignment="Center" Text="{x:Bind Lot.LOT_Quantite, Mode=OneWay, FallbackValue=''}" />
                            <!-- Cellule 3 : nom de la recette du lot, vide pour les événements -->
                            <TextBlock VerticalAlignment="Center" Text="{x:Bind Lot.REC_Nom, Mode=OneWay, FallbackValue=''}" />
                            <!-- Cellule 4 : libellé d'état du lot OU message de l'événement -->
                            <TextBlock VerticalAlignment="Center">
                                <Run Text="{x:Bind Lot.ETA_Libelle, Mode=OneWay, FallbackValue=''}" />
                                <Run Text="{x:Bind Evenement.EVE_Message, Mode=OneWay, FallbackValue=''}" />
                            </TextBlock>
                        </controls:DataRow>
                    </muxc:TreeViewItem>
                </DataTemplate>
            </controls:HeaderedTreeView.ItemTemplate>
        </controls:HeaderedTreeView>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="12">
            <Button
                x:Name="btnNouveauLot"
                Content="Nouveau Lot"
                Style="{StaticResource AccentButtonStyle}"
                Click="btnNouveauLot_Click" />
            <Button
                x:Name="btnModifierLot"
                Content="Modifier Lots"
                Style="{StaticResource DefaultButtonStyle}"
                Click="btnModifierLot_Click" />
        </StackPanel>
    </Grid>
</Page>
