<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="App_Gestion_lots_M3.UI.Pages.RecettesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:local="using:App_Gestion_lots_M3.UI.Pages"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Recettes" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock
                Text="Catalogue des recettes de fabrication"
                Style="{StaticResource BodyTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <InfoBar
            x:Name="StatusInfoBar"
            Grid.Row="1"
            IsClosable="True"
            IsOpen="False" />

        <!-- Même pattern que LotsPage : un seul DataTemplate sur
             RecetteTreeNode, propagation null via x:Bind. Lignes parent
             = Recette ; lignes enfants = Operation. -->
        <controls:HeaderedTreeView
            x:Name="treeRecettes"
            Grid.Row="2"
            SelectionMode="Single"
            DoubleTapped="treeRecettes_DoubleTapped"
            ItemsSource="{x:Bind _rows, Mode=OneWay}">
            <controls:HeaderedTreeView.Header>
                <controls:DataTable ColumnSpacing="16">
                    <controls:DataColumn Content="Nom de la Recette" DesiredWidth="Auto" />
                    <controls:DataColumn Content="Date / Position"   DesiredWidth="200" />
                    <controls:DataColumn Content="Nb Opé. / Temps"   DesiredWidth="160" />
                    <controls:DataColumn Content="Quittance"         DesiredWidth="*" />
                </controls:DataTable>
            </controls:HeaderedTreeView.Header>
            <controls:HeaderedTreeView.ItemTemplate>
                <DataTemplate x:DataType="local:RecetteTreeNode">
                    <muxc:TreeViewItem ItemsSource="{x:Bind Children}">
                        <controls:DataRow>
                            <!-- Cellule 1 : nom recette, vide pour les opérations -->
                            <TextBlock VerticalAlignment="Center" Text="{x:Bind Recette.REC_Nom, Mode=OneWay, FallbackValue=''}" />
                            <!-- Cellule 2 : date création (parent) OU position moteur (enfant) -->
                            <TextBlock VerticalAlignment="Center">
                                <Run Text="{x:Bind local:RecettesPage.FormatDate(Recette), Mode=OneWay, FallbackValue=''}" />
                                <Run Text="{x:Bind Operation.OPE_PositionMoteur, Mode=OneWay, FallbackValue=''}" />
                            </TextBlock>
                            <!-- Cellule 3 : nb opérations (parent) OU temps d'attente (enfant) -->
                            <TextBlock VerticalAlignment="Center">
                                <Run Text="{x:Bind local:RecettesPage.NbOperations(Recette), Mode=OneWay, FallbackValue=''}" />
                                <Run Text="{x:Bind local:RecettesPage.FormatTemps(Operation), Mode=OneWay, FallbackValue=''}" />
                            </TextBlock>
                            <!-- Cellule 4 : vide (parent) OU quittance Oui/Non (enfant) -->
                            <TextBlock VerticalAlignment="Center" Text="{x:Bind local:RecettesPage.FormatQuittance(Operation), Mode=OneWay, FallbackValue=''}" />
                        </controls:DataRow>
                    </muxc:TreeViewItem>
                </DataTemplate>
            </controls:HeaderedTreeView.ItemTemplate>
        </controls:HeaderedTreeView>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="12">
            <Button
                x:Name="btnNouvelleRecette"
                Content="Nouvelle Recette"
                Style="{StaticResource AccentButtonStyle}"
                Click="btnNouvelleRecette_Click" />
            <Button
                x:Name="btnModifierRecette"
                Content="Modifier Recette"
                Style="{StaticResource DefaultButtonStyle}"
                Click="btnModifierRecette_Click" />
        </StackPanel>
    </Grid>
</Page>
